{
  "name": "posteragent",
  "private": true,
  "workspaces": ["apps/*", "apps/nexus/apps/*", "apps/nexus/packages/*", "packages/*"],
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test": "turbo test",
    "generate": "turbo generate",
    "publish:all": "turbo publish:all",
    "factory:run": "turbo factory:run"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prettier": "^3.2.4",
    "turbo": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
