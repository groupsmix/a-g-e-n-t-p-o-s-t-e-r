{
  "name": "@posteragent/quota",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-1102 — Rate limit + API quota manager. Per-provider tracking, pre-flight checks, retry-next-window.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc", "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@posteragent/logger": "workspace:*" },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
