/**
 * Multi-Platform Publisher handler — registered for AgentTaskType 'publish'.
 *
 * Currently a Phase 3 stub.  Real implementation lands in Phase 7 (TASK-700).
 */
import { defineStub } from './_stub.js'

export const publishHandler = defineStub({
  type: 'publish',
  name: 'Multi-Platform Publisher',
  description: 'TikTok / IG / YT / X / Pinterest / LinkedIn with per-platform formatting.',
  phase: 'Phase 7 (TASK-700)',
})
