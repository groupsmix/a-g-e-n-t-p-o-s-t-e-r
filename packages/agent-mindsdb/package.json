{
  "name": "@posteragent/agent-mindsdb",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-1003 — MindsDB MCP client + unified cross-source query helpers (revenue × posts × leads).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc", "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@posteragent/logger": "workspace:*" },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^6.0.3",
    "vitest": "^1.2.0"
  }
}
