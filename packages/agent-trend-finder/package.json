{
  "name": "@posteragent/agent-trend-finder",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-403 — YouTube Trend Analyser. Fetches trending videos per niche, mines title/hook/thumbnail/velocity patterns, surfaces content gaps, and queues briefs to ContentAgent.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./adapters": "./src/adapters/index.ts",
    "./pipeline": "./src/pipeline/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@posteragent/logger": "workspace:*",
    "@posteragent/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
