{
  "name": "@posteragent/context-engine",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-405 — Context engineering pipeline. Wraps every agent invocation so every prompt is enriched with relevant memories, RAG hits over past task results, compressed prior context, and live system signals (date, goals, recent performance).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./stages": "./src/stages/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@posteragent/logger": "workspace:*",
    "@posteragent/memory": "workspace:*",
    "@posteragent/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
