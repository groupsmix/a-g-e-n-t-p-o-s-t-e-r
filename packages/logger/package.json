{
  "name": "@posteragent/logger",
  "version": "0.1.0",
  "private": true,
  "description": "Loggers for the posteragent monorepo: pino (Node) at ., zero-dep Workers-safe logger at ./workers (audit 2.3)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./workers": "./src/workers.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pino": "^8.18.0",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
