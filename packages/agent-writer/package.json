{
  "name": "@posteragent/agent-writer",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-601 — Writer Agent. Multi-format: blog, X thread, LinkedIn, IG, TikTok, YT, newsletter, product copy, cold email.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./adapters": "./src/adapters/index.ts",
    "./pipeline": "./src/pipeline/index.ts",
    "./formats": "./src/formats/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@posteragent/logger": "workspace:*",
    "@posteragent/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
