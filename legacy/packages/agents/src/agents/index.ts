import { trendAgent } from "./trend-agent.js";
import { queueAgent } from "./queue-agent.js";
import { posterAgent } from "./poster-agent.js";
import { siteContentAgent } from "./site-content-agent.js";

export { trendAgent, queueAgent, posterAgent, siteContentAgent };

export const allAgents = {
  trendAgent,
  queueAgent,
  posterAgent,
  siteContentAgent,
};
